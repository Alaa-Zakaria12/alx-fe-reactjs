import React from 'react';
import ReactDOM from 'react-dom/client'; // Vite uses ReactDOM.createRoot for rendering
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
